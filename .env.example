# Copy this file to .env and adjust values as needed
# These are overrides; defaults are already set in docker-compose.yml

# Secret key for signing tokens (change this!)
SECRET_KEY=change-me-in-production
